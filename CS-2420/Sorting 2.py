


def mergeSort(A):
	if len(A) <= 1:
		return
	m1 = A[] # Slice array and return first half
	m2 = A[] # Slice array and return second half
	mergeSort(m1)
	mergeSort(m2)
	# merge m1, m2
	m1i = 0
	m2i = 0
	Ai = 0
	while(A.len >= Ai):
		if m1[m1i] <= m2[m2i]:
			A[]
	return